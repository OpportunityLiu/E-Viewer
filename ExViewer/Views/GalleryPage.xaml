<Page x:Class="ExViewer.Views.GalleryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:ExViewer.Views"
      xmlns:controls="using:ExViewer.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      NavigationCacheMode="Enabled"
      x:Name="page">
    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <ContinuumNavigationTransitionInfo />
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <ScrollViewer x:Name="sv_Content"
                      Grid.Row="1"
                      VerticalSnapPointsType="Mandatory"
                      VerticalScrollMode="Auto"
                      VerticalScrollBarVisibility="Hidden"
                      Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
            <StackPanel>
                <Grid x:Name="gd_info"
                      Height="180">
                    <Grid.Background>
                        <LinearGradientBrush EndPoint="0.5,1"
                                             StartPoint="0.5,0">
                            <GradientStop Color="{ThemeResource SystemAltHighColor}"
                                          Offset="0" />
                            <GradientStop Color="{ThemeResource SystemChromeMediumColor}"
                                          Offset="1" />
                        </LinearGradientBrush>
                    </Grid.Background>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="4" />
                        <RowDefinition />
                        <RowDefinition Height="4" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Image Source="{x:Bind VM.Gallery.Thumb, Mode=OneWay}"
                           Grid.Row="1"
                           Margin="4" />
                    <RelativePanel Margin="4"
                                   Grid.Row="1"
                                   Grid.Column="1">
                        <controls:CategoryTag x:Name="Category"
                                              Category="{x:Bind VM.Gallery.Category, Mode=OneWay}"
                                              RelativePanel.AlignLeftWithPanel="True" />
                        <TextBlock x:Name="RecordCount"
                                   RelativePanel.RightOf="Category"
                                   RelativePanel.AlignVerticalCenterWith="Category"
                                   Text="{x:Bind VM.Gallery.RecordCount, Mode=OneWay, ConverterParameter=\{0\} Pages, Converter={StaticResource StringConverter}}" />
                        <TextBlock x:Name="Rating"
                                   RelativePanel.Below="Category"
                                   Text="{x:Bind VM.Gallery.Rating, Mode=OneWay, Converter={StaticResource RateStringConverter}}"
                                   FontFamily="Segoe MDL2 Assets" />
                        <ListView x:Name="lv_Tags"
                                  RelativePanel.Below="Rating"
                                  ItemsSource="{x:Bind VM.Gallery.Tags, Mode=OneWay}"
                                  ItemClick="lv_Tags_ItemClick"
                                  IsItemClickEnabled="True">

                        </ListView>
                    </RelativePanel>
                </Grid>
                <Grid x:Name="gd_Pivot">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Border Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}">
                        <Button x:Name="btn_Scroll"
                                Width="48"
                                Height="24"
                                Style="{StaticResource TextBlockButtonStyle}"
                                Content="&#xE014;"
                                FontFamily="Segoe MDL2 Assets"
                                AutomationProperties.Name="Show/Hide detiles"
                                Click="btn_Scroll_Click"
                                Padding="0"
                                HorizontalAlignment="Center"
                                HorizontalContentAlignment="Center"
                                RenderTransformOrigin="0.5,0.5">
                            <Button.RenderTransform>
                                <CompositeTransform Rotation="{x:Bind sv_Content.VerticalOffset, Mode=OneWay}" />
                            </Button.RenderTransform>
                        </Button>
                    </Border>

                    <Pivot x:Name="pv"
                           Grid.Row="1"
                           SelectionChanged="pv_SelectionChanged">
                        <PivotItem>
                            <PivotItem.Header>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Images" />
                                    <TextBlock Text="{x:Bind VM.Gallery.RecordCount, Mode=OneWay, Converter={StaticResource StringConverter}, ConverterParameter=[\{0\}]}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               VerticalAlignment="Top" />
                                </StackPanel>
                            </PivotItem.Header>
                            <GridView x:Name="gv"
                                      IsItemClickEnabled="True"
                                      ItemClick="gv_ItemClick"
                                      ItemsSource="{x:Bind VM.Gallery, Mode=OneWay}"
                                      SelectedIndex="{x:Bind VM.CurrentIndex, Mode=TwoWay}"
                                      VerticalAlignment="Top"
                                      Loaded="gv_Loaded">
                                <GridView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <ItemsWrapGrid HorizontalAlignment="Center"
                                                       Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </GridView.ItemsPanel>
                                <GridView.ItemTemplate>
                                    <DataTemplate xmlns:c="using:ExClient"
                                                  x:DataType="c:GalleryImage">
                                        <Image Source="{x:Bind Thumb, Mode=OneWay}"
                                               Width="100"
                                               MaxHeight="180" />
                                    </DataTemplate>
                                </GridView.ItemTemplate>
                                <GridView.Footer>
                                    <TextBlock HorizontalAlignment="Center"
                                               Margin="12"
                                               Style="{StaticResource BaseTextBlockStyle}"
                                               Visibility="{x:Bind VM.Gallery.HasMoreItems, Converter={StaticResource TrueToVisibleConverter}, Mode=OneWay}"
                                               Text="Loading more items..." />
                                </GridView.Footer>
                            </GridView>
                        </PivotItem>
                        <PivotItem>
                            <PivotItem.Header>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Comments" />
                                    <TextBlock Text="{x:Bind VM.CommentCount, Mode=OneWay, Converter={StaticResource StringConverter}, ConverterParameter=[\{0\}]}"
                                               Visibility="{x:Bind VM.CommentCount, Mode=OneWay, Converter={StaticResource NullToCollapsedConverter}}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               VerticalAlignment="Top" />
                                </StackPanel>
                            </PivotItem.Header>
                            <ListView x:Name="lv_Comments"
                                      ItemsSource="{x:Bind VM.Comments, Mode=OneWay}"
                                      VerticalAlignment="Top"
                                      Loaded="lv_Comments_Loaded">
                                <ListView.ItemTemplate>
                                    <DataTemplate xmlns:c="using:ExClient"
                                                  x:DataType="c:Comment">
                                        <StackPanel>
                                            <TextBlock Text="{x:Bind Content, Mode=OneWay}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                                <ListView.Header>
                                    <TextBlock HorizontalAlignment="Center"
                                               Margin="12"
                                               Style="{StaticResource BaseTextBlockStyle}"
                                               Visibility="{x:Bind VM.Comments.Count, Mode=OneWay, Converter={StaticResource ZeroToVisibleConverter}, FallbackValue=Collapsed}"
                                               Text="No comments" />
                                </ListView.Header>
                                <ListView.Footer>
                                    <TextBlock HorizontalAlignment="Center"
                                               Margin="12"
                                               Style="{StaticResource BaseTextBlockStyle}"
                                               Visibility="{x:Bind VM.Comments, Converter={StaticResource NullToVisibleConverter}, Mode=OneWay}"
                                               Text="Loading comments..." />
                                </ListView.Footer>
                            </ListView>
                        </PivotItem>
                        <PivotItem>
                            <PivotItem.Header>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Torrents" />
                                    <TextBlock Text="{x:Bind VM.TorrentCount, Mode=OneWay, Converter={StaticResource StringConverter}, ConverterParameter=[\{0\}]}"
                                               Visibility="{x:Bind VM.TorrentCount, Mode=OneWay, Converter={StaticResource NullToCollapsedConverter}}"
                                               Style="{StaticResource CaptionTextBlockStyle}"
                                               VerticalAlignment="Top" />
                                </StackPanel>
                            </PivotItem.Header>
                            <ListView x:Name="lv_Torrents"
                                      ItemsSource="{x:Bind VM.Torrents, Mode=OneWay}"
                                      VerticalAlignment="Top"
                                      SelectionChanged="lv_Torrents_SelectionChanged"
                                      Loaded="lv_Torrents_Loaded">
                                <ListView.ItemTemplate>
                                    <DataTemplate xmlns:c="using:ExClient"
                                                  x:DataType="c:TorrentInfo">
                                        <StackPanel>
                                            <TextBlock Text="{x:Bind Name, Mode=OneWay}" />
                                            <Grid x:Name="gd_TorrentDetail"
                                                  Visibility="Collapsed">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{x:Bind Posted, Converter={StaticResource StringConverter}, ConverterParameter=Posted: \{0\}}" />
                                                    <TextBlock Text="{x:Bind Uploader, Converter={StaticResource StringConverter}, ConverterParameter=Uploader: \{0\}}" />
                                                    <TextBlock Text="{x:Bind Downloads, Converter={StaticResource StringConverter}, ConverterParameter=Downloads: \{0\}}" />
                                                    <TextBlock Text="{x:Bind Peers, Converter={StaticResource StringConverter}, ConverterParameter=Peers: \{0\}}" />
                                                    <TextBlock Text="{x:Bind Seeds, Converter={StaticResource StringConverter}, ConverterParameter=Seeds: \{0\}}" />
                                                    <TextBlock Text="{x:Bind Size, Converter={StaticResource StringConverter}, ConverterParameter=Size: \{0\}}" />
                                                </StackPanel>
                                                <Button Command="{Binding VM.TorrentDownload, ElementName=page}"
                                                        CommandParameter="{x:Bind}"
                                                        Grid.Column="1">Download</Button>
                                            </Grid>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                                <ListView.Header>
                                    <TextBlock HorizontalAlignment="Center"
                                               Margin="12"
                                               Style="{StaticResource BaseTextBlockStyle}"
                                               Visibility="{x:Bind VM.Torrents.Count, Mode=OneWay, Converter={StaticResource ZeroToVisibleConverter}, FallbackValue=Collapsed}"
                                               Text="No available torrents" />
                                </ListView.Header>
                                <ListView.Footer>
                                    <TextBlock HorizontalAlignment="Center"
                                               Margin="12"
                                               Style="{StaticResource BaseTextBlockStyle}"
                                               Visibility="{x:Bind VM.Torrents, Converter={StaticResource NullToVisibleConverter}, Mode=OneWay}"
                                               Text="Loading torrents..." />
                                </ListView.Footer>
                            </ListView>
                        </PivotItem>
                    </Pivot>
                </Grid>
            </StackPanel>
        </ScrollViewer>
        <CommandBar x:Name="cb_top"
                    HorizontalContentAlignment="Stretch">
            <CommandBar.Content>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="48" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Button Style="{StaticResource NavigationBackButtonSmallStyle}"
                            Content="&#xE700;"
                            Height="48"
                            Width="48"
                            Click="btn_pane_Click" />
                    <TextBlock Text="{x:Bind VM.Gallery, Mode=OneWay, Converter={StaticResource GalleryToTitleStringConverter}}"
                               Style="{StaticResource BaseTextBlockStyle}"
                               Margin="8,0"
                               Grid.Column="1"
                               VerticalAlignment="Center"
                               ContinuumNavigationTransitionInfo.IsEntranceElement="True"
                               MaxLines="1"
                               TextTrimming="CharacterEllipsis"
                               TextWrapping="NoWrap">
                        <ToolTipService.ToolTip>
                            <TextBlock Text="{x:Bind VM.Gallery, Mode=OneWay, Converter={StaticResource GalleryToTitleStringConverter}}"
                                       TextWrapping="WrapWholeWords" />
                        </ToolTipService.ToolTip>
                    </TextBlock>
                </Grid>
            </CommandBar.Content>
            <CommandBar.SecondaryCommands>
                <AppBarButton x:Name="abb_open"
                              Label="Open in browser"
                              Command="{x:Bind VM.OpenInBrowser, Mode=OneWay}" />
                <AppBarButton x:Name="abb_save"
                              Label="Save this gallery"
                              Command="{x:Bind VM.Save, Mode=OneWay}" />
            </CommandBar.SecondaryCommands>
        </CommandBar>
        <ProgressBar x:Name="pb_save"
                     Grid.Row="1"
                     Foreground="{x:Bind VM.SaveStatus, Converter={StaticResource OperationStateToBrushConverter}, Mode=OneWay}"
                     Visibility="{x:Bind VM.SaveStatus, Converter={StaticResource OperationStateToVisibilityConverter}, Mode=OneWay}"
                     Maximum="1"
                     Value="{x:Bind VM.SaveProgress, Mode=OneWay}"
                     SmallChange="1E-05"
                     LargeChange="0.0001"
                     VerticalAlignment="Top" />
    </Grid>
</Page>
