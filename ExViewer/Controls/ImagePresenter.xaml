<UserControl x:Class="ExViewer.Controls.ImagePresenter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:ExViewer.Controls"
             xmlns:c="using:ExClient"
             xmlns:img="using:ImageLib.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="400">
    <ScrollViewer x:Name="sv"
                  ZoomMode="Enabled"
                  HorizontalScrollBarVisibility="Hidden"
                  VerticalScrollBarVisibility="Hidden"
                  MinZoomFactor="1"
                  Loading="sv_Loading"
                  ManipulationDelta="sv_ManipulationDelta"
                  PointerEntered="sv_PointerEntered"
                  PointerMoved="sv_PointerMoved"
                  PointerPressed="sv_PointerPressed"
                  ManipulationMode="All">
        <Grid HorizontalAlignment="Center"
              VerticalAlignment="Center"
              x:Name="gd_ContentRoot">
            <Image x:Name="img_Thumb"
                   Source="{x:Bind Image.Thumb, Mode=OneWay}"
                   HorizontalAlignment="Stretch"
                   VerticalAlignment="Stretch" />
            <ContentControl Content="{x:Bind Image, Mode=OneWay}"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch">
                <ContentControl.ContentTemplateSelector>
                    <local:ImagePresenterSelector>
                        <local:ImagePresenterSelector.Template>
                            <DataTemplate x:DataType="c:GalleryImage">
                                <Image Source="{x:Bind Image, Mode=OneWay}"
                                       Stretch="Uniform" />
                            </DataTemplate>
                        </local:ImagePresenterSelector.Template>
                        <local:ImagePresenterSelector.GifTemplate>
                            <DataTemplate x:DataType="c:GalleryImage">
                                <img:ImageView UriSource="{x:Bind ImageFileUri, Mode=OneWay}"
                                               Stretch="Uniform" />
                            </DataTemplate>
                        </local:ImagePresenterSelector.GifTemplate>
                    </local:ImagePresenterSelector>
                </ContentControl.ContentTemplateSelector>
            </ContentControl>
        </Grid>
    </ScrollViewer>
</UserControl>
