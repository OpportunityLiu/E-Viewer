<UserControl x:Class="ExViewer.Controls.ImagePresenter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:ExViewer.Controls"
             xmlns:c="using:ExClient"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Loaded="UserControl_Loaded"
             Unloaded="UserControl_Unloaded"
             d:DesignHeight="300"
             d:DesignWidth="400">
    <UserControl.Resources>
        <DataTemplate x:Key="GifTemplate"
                      xmlns:img="using:ImageLib.Controls"
                      x:DataType="c:GalleryImage">
            <img:ImageView UriSource="{Binding ImageFileUri, Mode=OneWay}"
                           Stretch="Uniform" />
        </DataTemplate>
        <DataTemplate x:Key="NormalTemplate"
                      x:DataType="c:GalleryImage">
            <Image Source="{Binding Image, Mode=OneWay}"
                   Stretch="Uniform" />
        </DataTemplate>
        <local:ImagePresenterSelector x:Key="ImageTemplateSelector"
                                      Template="{StaticResource NormalTemplate}"
                                      GifTemplate="{StaticResource GifTemplate}" />
    </UserControl.Resources>
    <ScrollViewer x:Name="sv"
                  IsTabStop="True"
                  ZoomMode="Enabled"
                  Template="{StaticResource ScrollViewerScrollBarlessTemplate}"
                  HorizontalScrollBarVisibility="Hidden"
                  VerticalScrollBarVisibility="Hidden"
                  MinZoomFactor="1"
                  Loading="sv_Loading"
                  ManipulationDelta="sv_ManipulationDelta"
                  PointerEntered="sv_PointerEntered"
                  PointerMoved="sv_PointerMoved"
                  PointerPressed="sv_PointerPressed"
                  ManipulationMode="All">
        <Grid HorizontalAlignment="Center"
              VerticalAlignment="Center"
              x:Name="gd_ContentRoot">
            <Image x:Name="img_Thumb"
                   Source="{x:Bind Image.Thumb, Mode=OneWay}" />
            <ContentControl x:Name="cc_Image"
                            Content="{x:Bind Image, Mode=OneWay}"
                            ContentTemplateSelector="{StaticResource ImageTemplateSelector}"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch"
                            VerticalContentAlignment="Stretch"
                            IsTabStop="False" />
        </Grid>
    </ScrollViewer>
</UserControl>
